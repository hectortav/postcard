# Node / pnpm
node_modules/
.pnpm-store/
.pnpm-debug.log*
pnpm-debug.log*

# Build outputs (TS / Vite / Turbo)
dist/
build/
.turbo/
.turbo-cache/
*.tsbuildinfo

# Gradle / Java
.gradle/
target/
*.jar
*.war
hs_err_pid*
replay_pid*
# Exception: commit the Gradle wrapper jar so `./gradlew` works from a fresh clone.
!gradle/wrapper/gradle-wrapper.jar
!apps/cli-server/gradle/wrapper/gradle-wrapper.jar

# Vite build output for the Java resource bundle — generated, not committed
apps/cli-server/src/main/resources/public/
apps/cli-server/src/main/resources/public/**

# GraalVM native-image build output
apps/cli-server/build/
apps/cli-server/.cxx/

# IDE / editor
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
*.swp
*.swo

# Local env / secrets — never committed
.env
.env.local
.env.*.local
**/application-local.yml
**/application-local.properties

# Test / coverage
coverage/
.nyc_output/
playwright-report/
test-results/
/blob-report/
/playwright/.cache/

# OS
.DS_Store
.AppleDouble
.LSOverride
Icon[\r]
ehthumbs.db
Desktop.ini

# Icon rasterisation intermediates (regenerate from icons/*.svg)
apps/cli-server/icons/png/

# Superpowers working notes (specs, plans, skill state). Kept locally, never committed.
.superpowers/
docs/superpowers/
