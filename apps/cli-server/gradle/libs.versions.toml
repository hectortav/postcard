[versions]
javalin = "6.7.0"
# Javalin 6.3.0 owns the Jetty version via its jetty-bom import (Jetty 11.0.23).
# We do not pin Jetty here because the brief's "12.0.10" pin conflicts with Javalin's
# transitive: jetty-servlet has no 12.x publication and jetty-core has no 11.x
# publication (Jetty renamed/removed modules between 11.x and 12.x). Letting
# Javalin's BOM govern is the minimal fix.
picocli = "4.7.6"
zxing = "3.5.3"
slf4j = "2.0.17"
logback = "1.5.20"
# Jackson is optional in Javalin 6.3.0 (declared `<optional>true</optional>` in its POM), and
# postcard uses it directly: the WebSocket clipboard handler parses `{type:"clipboard",...}`
# frames with ObjectMapper.readValue, and the PIN routes parse their request bodies. That is
# attacker-reachable input from any device on the network, which makes this the single
# dependency most worth keeping current -- it was pinned to a 2022 build to match Javalin's
# own transitive, and held there.
jackson = "2.18.2"
junit = "5.11.4"
mockito = "5.14.2"
# shadow 8.1.1 (in the brief) was not published; the 8.x line starts at 8.3.0.
# 8.3.5 was incompatible with the Gradle 9 application plugin (mainClassName removal),
# so we use the first 9.x release, which is the line aligned with Gradle 9.
shadow = "9.0.0"
# JCEF (embedded Chromium) via jcefmaven. The version tracks the Chromium major:
# 146.0.10 is the current release. Natives are downloaded once at build time by the
# `installCefNatives` task and bundled into the installer, so the app itself runs with
# setSkipInstallation(true) and never touches the network on first run.
jcef = "146.0.10"

[libraries]
javalin = { module = "io.javalin:javalin", version.ref = "javalin" }
# Jetty is pulled in transitively via Javalin's jetty-bom.
picocli = { module = "info.picocli:picocli", version.ref = "picocli" }
zxing-core = { module = "com.google.zxing:core", version.ref = "zxing" }
zxing-javase = { module = "com.google.zxing:javase", version.ref = "zxing" }
slf4j-api = { module = "org.slf4j:slf4j-api", version.ref = "slf4j" }
logback-classic = { module = "ch.qos.logback:logback-classic", version.ref = "logback" }
jackson-databind = { module = "com.fasterxml.jackson.core:jackson-databind", version.ref = "jackson" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }
mockito-core = { module = "org.mockito:mockito-core", version.ref = "mockito" }
jcefmaven = { module = "me.friwi:jcefmaven", version.ref = "jcef" }

[plugins]
shadow = { id = "com.gradleup.shadow", version.ref = "shadow" }
