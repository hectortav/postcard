// Placeholder tokens stub. Replaced by Task 1.2 with the real StyleX design tokens.
// Shape mirrors the keys consumed by App.tsx so the bootstrap typechecks.
export const tokens = {
  colors: {
    bg: '#000000',
  },
  space: {
    lg: '16px',
  },
  font: {
    sans: 'system-ui, sans-serif',
  },
} as const;
