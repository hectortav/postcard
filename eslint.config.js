// TODO(Task 0.3): replace with `import base from '@sendme/config-eslint'; export default base;`
// Placeholder so `pnpm install` and downstream tasks can proceed before the shared config package exists.
export default [];
